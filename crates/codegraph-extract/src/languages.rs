//! Per-language extractor registry. One module per language behind a feature.
//!
//! TODO: trait Extractor { fn language() -> tree_sitter::Language; fn extract(...) -> ...; }
//! and one impl per supported language.
